---
nfs::client_enabled: true
nfs::client_mounts:
  /net/obs-env:
    share: "obs-env"
    server: "nfs-obsenv.ls.lsst.org"
    atboot: true
  /net/scratch:
    share: "scratch"
    server: "nfs-scratch.ls.lsst.org"
    atboot: true

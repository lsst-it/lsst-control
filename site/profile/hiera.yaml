---
version: 5
defaults:
  datadir: "data"
  data_hash: "yaml_data"
hierarchy:
  - name: "common"
    paths:
      - "osfamily/%{os.family}/major/%{os.release.major}.yaml"
      - "osfamily/%{os.family}.yaml"
      - "common.yaml"

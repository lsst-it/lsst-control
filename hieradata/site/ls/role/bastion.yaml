---
classes:
  - "profile::core::nfsclient"
  - "profile::core::icinga_agent"

nfs::client_enabled: true
nfs::client_mounts:
  /net/lsstdata/admin:
    share: "lsstdata/admin"
    server: "ddn-nfs.ls.lsst.org"
    atboot: true

profile::core::icinga_agent::host_template: "GeneralHostTemplate"
